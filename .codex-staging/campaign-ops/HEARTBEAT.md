# Campaign Operations build heartbeat

Read `TASK.md` and `PROGRESS.md`. Inspect the repo and linked issue. If acceptance criteria remain, complete the highest-value safe increment, verify it, update `PROGRESS.md`, then commit and push a coherent checkpoint. If implementation is complete, run a fresh review/build and fix any real defect you find. Never send real email, merge, deploy, or claim simulated actions occurred. Reply with a concise shipped/verified/next summary; use `HEARTBEAT_OK` only when the scoped work is complete and a fresh verification finds no action.
